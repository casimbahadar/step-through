# Handoff addendum: 2026-08-10 (Fable 5 session)

## What happened
The Opus 5 session ended; its container (and current source files) are gone.
The uploaded files were a mix of vintages. Audit result:

- **Current**: `polyglot.html` (the shipped artifact: source of truth),
  `tests-shipped.mjs` (green against it), `PROJECT-STATE.md`.
- **Stale** (earlier-session versions, do NOT build from them): the originally
  uploaded `engine.mjs` (132 KB vs 170 KB shipped), `game.mjs`, `content.mjs`,
  `ui.html`, `build.py`, `content-tests.mjs`, `smoke.mjs`, `tests.mjs`.
  The stale versions predate: the markup track, `scaffold`/`starterFor` (tasks
  no longer carry per-language `starter` fields), PATTERN_NAMES/RECOGNISE,
  translateWithMap/inferTypes, measureGrowth, makeRecognitionRound.
- **Lost with the old session**: current `loadparts.mjs`, `markup-oracle.mjs`,
  `ui-browser.mjs`, `preflight.mjs`, and the current versions of
  `content-tests.mjs` and `smoke.mjs`.

## Recovery (verified)
- The src tree in this folder (`engine.mjs`, `game.mjs`, `content.mjs`,
  `markup.mjs`, `ui.html`, `build.py`) was **regenerated from the shipped
  file's marker blocks**. `build.py` was updated to add the MARKUP block and
  the current POLYGLOT tail. Proof: `python3 build.py` on this tree rebuilds
  `polyglot.html` **byte-identical** (sha256 31c020a3…f190 on both).
  Caveat: `export` prefixes were stripped at the original build, so these
  regenerated .mjs files carry none; `loadparts.mjs` (marker extraction) does
  not need them, but the old import-based `tests.mjs` src mode would.
- `loadparts.mjs` here is a **reconstruction** (interface derived from its
  callers + build.py). Marked as such in its header.

## Verification run this session (all against the shipped file)
- `node tests-shipped.mjs` → 2168 pass / 0 fail (2025 V8-oracle cases).
- `node stopgap-content.mjs` → 328 pass / 0 fail: all 82 graded solutions
  produce `expect` in Python and translated JS (Project stages use their
  scripted `inputs`), and all 164 generated starters (starterFor, both
  languages) fail to produce the answer: the no-freebie rule holds.
- `node xss-probe.mjs` → hostile HTML in program output/strings renders as
  text, never markup; no elements injected, no handlers fired. CORE and GAME
  contain no eval/new Function/localStorage/fetch.

## Not yet restored
Full curriculum harness (hints/craft/questions/demos sections), smoke
playthrough, preflight, markup oracle, browser driver: need porting to the
current shapes. Multi-language compiler oracle needs toolchains reinstalled.

## Session 2 additions (same day, after "continue with option 1")
- markup-oracle.mjs and ui-browser.mjs recovered VERBATIM from the old chat's
  transcript via conversation search (ui-browser needed two strings synced to
  the localized curriculum). Chromium method recovered the same way.
- content-tests.mjs fully ported (sections A-G kept, H recognition drill and
  the field-name lint added, solutions/demos/starters checked in all nine
  languages in-engine). smoke.mjs synced to localized content and the
  checkpoint-shifted lesson indices; view-only assertions replaced with
  all-languages-editable assertions. preflight.mjs rebuilt.
- Engine fixes shipped and oracle-verified: kotlin size-field collision
  (content rename + lint), csharp empty base-call ctor, ruby gets().chomp,
  empty-list inference default moved to print time + append element-type flow.
- multi-oracle: restored from the handoff doc, then extended with the
  documented [langs] argument, scripted stdin, and the TS prompt() shim , 
  this file is a reconstruction+extension, not the old session's final copy.
- Final board (one build, everything re-run on it): 2168 + 10578 + 180 + 63 +
  12 + 8 + 734 = 13,743 assertions, 0 failures.
